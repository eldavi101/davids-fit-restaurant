<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        HTTPS is required everywhere. The single exception is the emulator loopback host,
        so a developer can point the app at a backend running on their own machine without
        weakening transport security for any real deployment (requirement 52).
    -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
